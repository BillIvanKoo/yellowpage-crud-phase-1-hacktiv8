<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Add Business Contact</title>
  </head>
  <body>
    <form action="" method="post">
      Name <input type = "text" name = "name" values ="<%=contact.name%>">
      Phone<input type = "text" name = "phone" values = "<%contact.phone%>">
      Email<input type = "text" name = "Email" values = "<%contact.email%>">
      Description<input type = "textbox" name = "description" values = "<%=contact.description%>">
      <select name = "" required>
      <% for(let i = 0; i < categories.length;i++){
        <option value = "<%= categories[i].id%>"></option>
      </select>
      <select name = "" required>
      <% for(let i = 0; i < cities.length;i++){
        <option value = "<%= cities[i].id%>"></option>
      </select>
      <button type = "submit">Submit</button>
    </form>
  </body>
</html>
